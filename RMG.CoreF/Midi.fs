namespace RMG.CoreF

//module Midi
//
//    let writeSong (stream: Stream) (song: Song): ReadOnlySpan<byte> =
//        let renderTrack (track: Track, noteMap: NoteOffsetStateBasedEventTimeline<NoteOffset>) =
//
